{
  "name": "House Party",
  "description": "House party clone using Django and React, deployed to Heroku",
  "image": "heroku/python",
  "repository": "https://github.com/naistangz/house-party",
  "keywords": ["python", "django" ],
  "addons": [ "heroku-postgresql" ],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "python manage.py collectstatic --noinput",
        "test": "python manage.py test"
      }
    }
  }
}
